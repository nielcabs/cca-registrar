<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head') %>
    <title>CCA Registrar System - Login</title>
  </head>
  <body class="auth-bg">
    <main class="card auth-card">
      <%- include('partials/auth-header') %>
      <h1>Welcome back</h1>
      <p class="muted auth-lead">Document requisition, clearance verification &amp; scheduling</p>

      <% if (error) { %>
      <div class="alert error"><%= error %></div>
      <% } %>
      <% if (success) { %>
      <div class="alert success"><%= success %></div>
      <% } %>

      <form method="post" action="/login" class="form">
        <label>
          Email
          <input type="email" name="email" placeholder="you@cca.edu.ph" required autofocus />
        </label>
        <label>
          Password
          <input type="password" name="password" required />
        </label>
        <button type="submit">Sign in</button>
      </form>

      <p class="tiny muted auth-links">
        <a href="/forgot-password">Forgot password?</a>
      </p>

      <p class="tiny muted auth-links">
        New student? <a href="/register">Create an account</a>
      </p>
    </main>
  </body>
</html>
